[package]
name         = "arc-labs-bootstrap"
description  = "OS and distro detection, environment diagnosis (doctor) and dependency installation (setup). The ONLY crate permitted an outbound network client."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[dependencies]
thiserror = { workspace = true }
serde     = { workspace = true }
tracing   = { workspace = true }
which     = { workspace = true }
arc-labs-core.workspace = true

[dev-dependencies]
tempfile = "3.27.0"
